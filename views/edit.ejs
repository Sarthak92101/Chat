<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit yout chat</title>
</head>
<body>
  <style>
    body {
  background-color: #f7f7f7;
  font-family: Arial, sans-serif;
  padding: 20px;
}

h3 {
  color: #333;
}

textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

button {
  background: #4caf50;
  color: white;
  border: none;
  padding: 8px 15px;
  border-radius: 5px;
  cursor: pointer;
}

  </style>
  <h3>Edit your chat <%=chat._id %></h3>
<p>This chat sent from <b><%= chat.from %></b> to <b><%= chat.to %></p></b>
  <form method="POST" action="/chats/<%= chat._id%>?_method=PUT">
    <textarea name="msg" rows="" cols="50"><%= chat.msg %></textarea>
    <button>Edit</button>
  </form>
</body>
</html>